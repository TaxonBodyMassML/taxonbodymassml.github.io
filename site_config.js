// Organization-owned deployment settings for the static frontend.
// Override API_BASE_URL per environment if the backend URL changes.
window.TAXON_CONFIG = {
  API_BASE_URL: "https://haileystaxonbodymassml.onrender.com"
}
